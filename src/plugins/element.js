import Vue from 'vue'
import {
    Button,
    Container,
    Aside,
    MenuItem,
    Menu,
    Header,
    Dropdown,
    DropdownMenu,
    DropdownItem,
    Table,
    TableColumn,
    MenuItemGroup,
    Submenu,
    Main
} from 'element-ui'

Vue.use(Button).use(Container).use(Aside).use(MenuItem).use(Menu).use(Header).use(Dropdown).use(DropdownMenu).use(DropdownItem).use(Table).use(TableColumn).use(MenuItemGroup).use(Submenu).use(Main)